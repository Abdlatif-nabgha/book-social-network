apiVersion: v1
kind: Service
metadata:
  name: bsn-api
  namespace: bsn
  labels:
    app: bsn-backend
spec:
  type: ClusterIP
  selector:
    app: bsn-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
